# @tailwindcss/oxide declares oxide-wasm32-wasi as a URL dependency. npm 12
# refuses those by default (allow-remote=none) and the install fails with
# EALLOWREMOTE, so allow them for this project.
allow-remote=all
